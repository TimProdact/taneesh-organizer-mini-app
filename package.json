{
  "name": "taneesh-organizer-mini-app",
  "private": true,
  "type": "module",
  "scripts": {
    "dev:mini-app": "npm run dev --prefix mini-app",
    "build:mini-app": "node -e \"console.log('Mini App = prebuilt cabinet in public/mini-app (npm run build:tg-app from Taneesh.org)')\"",
    "setup:telegram": "node scripts/setup-telegram.mjs",
    "deploy": "node scripts/deploy-render.mjs",
    "deploy:netlify": "node scripts/deploy-netlify.mjs",
    "start": "node server/api-server.mjs",
    "deploy:yougile": "node scripts/set-yougile-render-env.mjs"
  },
  "dependencies": {
    "@netlify/blobs": "^9.1.2",
    "@telegram-apps/init-data-node": "^2.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
